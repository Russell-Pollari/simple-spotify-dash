{
  "name": "spotify-dash",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild client/App.jsx --bundle --minify --sourcemap --target=es2015 --outfile=dist/index.js",
    "watch": "esbuild client/App.jsx --bundle --minify --sourcemap --target=es2015 --outfile=dist/index.js --watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@emotion/react": "^11.11.0",
    "@emotion/styled": "^11.11.0",
    "@mui/material": "^5.13.4",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.17.19"
  }
}
